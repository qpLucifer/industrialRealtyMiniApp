/** Re-export — WeChat MP loads utils via request.js shim; keep imports stable in source. */
export {
  applyPropertyApiForm,
  buildPropertySubmitPayload,
  chipListFromJoined,
  emptyPropertyForm,
  joinMediaLines,
  parseMediaLines,
} from '@/utils/request'
